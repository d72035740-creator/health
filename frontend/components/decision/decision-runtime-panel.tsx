"use client";
import { useEffect, useState } from "react";
type D={adi:number|null; surveillance_state:string; components:Record<string,number>|null; modifiers:Record<string,number>|null; explanation_codes:string[]}|null;
const api=process.env.NEXT_PUBLIC_API_BASE_URL??"http://localhost:8000";
export function DecisionRuntimePanel({backendConnected}:{backendConnected:boolean}){const[d,setD]=useState<D>(null);useEffect(()=>{if(!backendConnected)return;const f=async()=>{try{setD(await(await fetch(`${api}/api/v1/decision/latest`)).json())}catch{}};void f();const id=window.setInterval(()=>void f(),3000);return()=>window.clearInterval(id)},[backendConnected]);return <section className="mt-5 rounded-2xl border border-[var(--line)] bg-[rgba(14,31,37,.68)] p-5"><p className="text-[10px] font-semibold uppercase tracking-[.2em] text-[var(--amber)]">Aequor surveillance decision engine</p><div className="mt-3 flex flex-wrap items-end gap-6"><div><p className="text-[10px] uppercase tracking-wider text-[var(--muted)]">Aequor differential index</p><p className="text-4xl text-white">{d?.adi==null?"—":`${d.adi.toFixed(1)} / 100`}</p><p className="text-[10px] uppercase tracking-wider text-[var(--muted)]">Prototype research index · not a disease probability</p></div><div><p className="text-[10px] uppercase tracking-wider text-[var(--muted)]">Surveillance state</p><p className="text-lg text-[#b7cbc8]">{d?.surveillance_state??"CALIBRATING"}</p></div></div></section>}
